# Completing the refactoring plan

Follow-up work is tracked in [the follow-up report](2026-09-22-followup-results.md). The source09 results below remain historical evidence.

Latest source14: deeper broker/client extraction, owned workload profiles, private reporting, the actual manager fixture and linked TSan checks are delivered. The final 53-target Release suite, minimal build, 12 publisher ASan/UBSan tests, seven linked TSan fixtures and four final owned runs passed. The separate source11 campaign passed 23 fault cases and 52 matched runtime trials; one-broker primary performance remains inconclusive. Consult the [current closure audit](2026-09-22-plan-closure-audit.md) for remaining software and research gates; the table below is the earlier source09 record.

Status: the bounded completion tranche is implemented, independently reviewed, and validated as recorded in the [final evidence](2026-09-22-completion-results.md). The wider release/research roadmap is not fully closed. This is a completion ledger for the existing [twelve-part plan](2026-09-22-refactoring-plan.md), not a replacement of its correctness or performance requirements. Preserve the prior compiled-source archive and all failed and qualified performance campaigns. Existing user edits to README, ARTIFACT and the skew experiment plan are not owned by this work.

The owner selected a bounded single-host research prototype: retain data and stop safely at capacity, with no log recycling or eviction of replayable data to accommodate slow subscribers. The owner also directed use of the same project license as Ray and vLLM: [Apache-2.0](../../LICENSE), attributed to The Embarcadero Authors in [NOTICE](../../NOTICE). These two decisions are resolved. Missing CXL hardware prevents hardware qualification, not DRAM development.

| Work package | Concrete deliverable | Acceptance gate | State |
|---|---|---|---|
| A: coordinated startup (PR03/07) | Extract safe mapping/attachment; followers use the ready head descriptor's base; eliminate clobber fallback; clean actionable startup errors | Deterministic occupied-address, mismatch, timeout and sentinel tests; automatic-base real cluster startup; sanitizer checks | Implemented and independently reviewed; component ASan/UBSan and final one/three-broker automatic attachment passed with exact audits and normal cleanup |
| B: production fault evidence (PR04–07) | Compile-disabled local barrier controller, native wire driver and owned integration campaign; real ingress, shutdown, classification/fence/commit and ACK retirement cases | Execute the twelve initial fault-plan cases and boundary/lifetime extensions; exact prefix/payload, no false progress, normal cleanup; failed schedules retained | All 20 final immutable-binary cases passed, with exact oracles and owned normal cleanup; independent evidence review recorded |
| C: support and storage contracts (PR04–09) | Central mode/admission validation, explicit ACK/sink and session lifetime semantics, finite capacity behavior, declared retention ownership | Reject nonfunctional combinations and impossible replication before publication; production exhaustion leaves readable prefix; no silent RF clamping or fabricated durability | Implemented/reviewed; support matrix and sink tests passed; final live capacity/token cases passed |
| D: component extraction (PR10) | Isolate mapping, wire protocol, admission, session transitions, capacity and client dependencies along production paths | Focused production tests and differential protocol observations; no new hot-path payload copies or abstraction locks; extracted components actually linked | Implemented/reviewed; four extracted session method bodies byte-identical to characterized version; linked full/minimal builds passed |
| E: reproducibility/tooling/release (PR01/02/11/12) | Minimal client without baseline dependencies, optional baseline graph, one owned orchestration layer for supported local profiles, clean bootstrap/CI, provenance/security/contribution/release docs | Actual isolated build and required test inventory; safe documented entrypoints; source/dependency evidence; owner-selected license | Clean Ubuntu rootfs full build, 47/47 CTests and minimal client passed; CI configured; Apache-2.0 selected and added; private reporting setup remains open |
| F: integrated review and measurement | Cross-review each author's changes, final full build/tests/sanitizers, one/three-broker audits, qualified before/after comparison after runtime changes | Record exact source, flags, modes, topology, success/failure and uncertainty; no reuse of old performance evidence for changed runtime binaries | Cross-review, focused ASan/UBSan, and all 10 linked publisher sanitizer tests passed; final broker09/client08 builds frozen with matched source/dependency evidence; 20/20 final faults, all three ordinary smokes, and all 28 matched performance trials passed correctness/cleanup gates; three-broker 5% nonregression signal, one-broker primary endpoint inconclusive |

The test controller is compiled only into explicit fault builds. The normal build has no control listener, environment polling or injected clock/capacity behavior. Local fault channels use inherited owned UNIX socket FDs, bounded commands and one-shot selectors, explicit reached/release events, and stop/disconnect cancellation. A missing reached event fails the schedule. Startup synchronization orders initial arming before any hook proceeds; later arming requires an earlier reached barrier or other explicit proof that the transition has not occurred.

Live clusters run serially under the existing ownership lock. Use explicit DRAM emulation, a fresh 64 GiB region and checked geometry, node-1 broker memory/CPUs and node-0 clients, bounded workload/log/memory budgets, and owned cleanup. Small component fixtures do not substitute for the real-path campaign. The [fault specification](2026-09-22-production-fault-plan.md) defines invariants and the distinction between reachable live sequencing and stronger synthetic schedules.

Every work package receives independent review before its completion claim. Update this ledger with artifacts and exact remaining limitations; do not equate implemented test infrastructure, unexecuted CI configuration, passing helper tests, or an unresolved hardware/reporting prerequisite with completion.

The original roadmap's broader software work remains explicit: deeper ordering/client state-machine extraction, migration of historical experiment launchers beyond the supported owned entrypoints, stronger fault schedules listed in the fault specification, and successful TSan/full-cluster instrumentation. Small-message, fixed-load latency, and server-capacity studies are not supplied by the finite single-session throughput pilot. Those are software/measurement follow-ups, not work blocked solely by the missing CXL device. Safe reuse, replicated-metadata recovery, and the offset-only shared representation also remain outside this bounded prototype. Private reporting setup and real-CXL qualification remain separate release gates. The license and bounded retention contract are selected; safe reuse is not part of the chosen release scope.
